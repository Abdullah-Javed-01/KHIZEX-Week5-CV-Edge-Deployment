# Notebooks

The assignment permits either a complete notebook **or** a structured `.py` pipeline. This submission uses `src/pipeline.py` as the reproducible primary workflow, so no notebook is required.
