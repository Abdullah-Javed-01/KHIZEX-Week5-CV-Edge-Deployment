# KHIZEX Week 5 — Computer Vision Edge Deployment

**Author:** Abdullah Javed

## Project goal
Compress a trained image-classification model with **pruning** and **quantization**, then compare accuracy, model size, and real CPU inference latency. This project follows the Week 5 brief while using a CPU-only simulation because dedicated edge hardware is not available.

## Dataset
Public `sklearn` Digits dataset: 1,797 grayscale 8×8 images, 10 classes. Fixed stratified 75/25 train/test split, seed 42.

## Edge scenario and constraints
Target: a low-power classroom/embedded digit-recognition camera on a Raspberry-Pi-class CPU.

- model on disk: **≤ 2.5 MB**
- median single-image latency: **≤ 5 ms** in the CPU simulation
- classification accuracy: **≥ 95%**
- power envelope: **~5 W budget** (scenario estimate; not directly measured in this environment)

The baseline is intentionally over-parameterized so compression has a meaningful deployment role.

## Methods
- **Baseline:** FP32 MLP image classifier.
- **Pruning:** 50% L1 unstructured pruning of Linear-layer weights, followed by a short recovery fine-tune and re-application of 50% sparsity.
- **Quantization:** dynamic INT8 quantization of Linear layers using PyTorch.
- **Combined:** pruned model + dynamic INT8 quantization.
- **Runtime benchmark:** same CPU, one Torch thread, fixed test set, 50 warmups + 500 single-image timed runs; median and p95 reported.

> **Runtime note:** The measured results below are from PyTorch/TorchScript CPU. This environment does not have the Python `onnx` / `onnxruntime` packages installed, so no ONNX Runtime numbers are claimed. `src/export_onnx.py` is a runnable final edge-runtime pass for the exact checkpoints generated by `src/pipeline.py`.

## Results

| Variant | Accuracy | Model size | Parameters | Median latency | p95 latency | Size reduction | Accuracy drop |
|---|---:|---:|---:|---:|---:|---:|---:|
| Baseline FP32 | 98.67% | 8.566 MB | 2,241,546 | 0.391 ms | 0.518 ms | 0.00% | 0.00 pp |
| Pruned FP32 | 98.22% | 8.566 MB | 2,241,546 | 0.378 ms | 0.517 ms | ~0.00% | 0.44 pp |
| Quantized INT8 | 98.67% | 2.169 MB | 2,241,546 | 0.158 ms | 0.241 ms | 74.67% | 0.00 pp |
| Pruned + Quantized INT8 | 98.22% | 2.170 MB | 2,241,546 | 0.156 ms | 0.234 ms | 74.67% | 0.44 pp |

Pruned accuracy **before recovery:** 97.78%  
Final measured pruning sparsity: **50.00%**

## Recommendation
For the stated CPU-simulation constraints, the selected deployment candidate is **Quantized INT8**. It meets the size, latency, and accuracy targets while preserving baseline accuracy. See `ANALYSIS.md` for the full trade-off reasoning.

## Project structure
```text
KHIZEX-Week5-CV-Edge-Deployment/
├── data/
│   └── README.md
├── models/
│   └── README.md          # generated model artifacts are written here
├── notebooks/
│   └── README.md
├── results/
│   ├── metrics_summary.csv
│   ├── metrics_summary.json
│   ├── runtime_info.json
│   ├── accuracy_vs_latency.png
│   └── accuracy_vs_size.png
├── src/
│   ├── pipeline.py
│   └── export_onnx.py
├── ANALYSIS.md
├── README.md
├── requirements.txt
└── .gitignore
```

## Setup
```bash
python -m venv .venv
# Windows:
.venv\Scripts\activate
# Linux/macOS:
source .venv/bin/activate

pip install -r requirements.txt
python src/pipeline.py
```

Generated model binaries are intentionally not versioned because they are reproducible build artifacts. `python src/pipeline.py` recreates the FP32, pruned, INT8, and combined artifacts in `models/`.

For the strict ONNX-runtime final pass:

```bash
python src/export_onnx.py
```

The script exports baseline/pruned ONNX models, creates INT8 ONNX variants, benchmarks all four with ONNX Runtime, and writes ONNX-specific metrics and plots.
